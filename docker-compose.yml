version: '3.8'

services:
  client:
    build:
      context: ./client
    ports:
      - "3005:80"
    restart: always

  server:
    build:
      context: ./server
    ports:
      - "5005:5005"
    env_file:
      - ./server/.env
    restart: always
